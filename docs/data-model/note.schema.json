{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nexusforge.app/schemas/note.schema.json",
  "title": "Note",
  "type": "object",
  "required": ["id", "scope", "type", "content", "createdAt", "createdByUserId"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Identifiant unique de la note (UUID)."
    },

    "scope": {
      "type": "string",
      "enum": ["campaign", "session", "character", "npc", "location", "item", "other"],
      "description": "Contexte principal de la note (campagne, session, PJ, PNJ, etc.)."
    },

    "scopeRefId": {
      "type": ["string", "null"],
      "description": "Identifiant de la ressource liée (campaignId, sessionId, characterId, etc.)."
    },

    "type": {
      "type": "string",
      "enum": ["player_private", "gm_private", "public"],
      "description": "Type de note : privée joueur, privée MJ ou publique."
    },

    "title": {
      "type": "string",
      "description": "Titre court optionnel de la note."
    },

    "content": {
      "type": "string",
      "description": "Contenu de la note (markdown ou texte brut)."
    },

    "createdByUserId": {
      "type": "string",
      "description": "Utilisateur qui a créé la note."
    },

    "ownerUserId": {
      "type": ["string", "null"],
      "description": "Propriétaire de la note (obligatoire pour player_private, souvent null pour public)."
    },

    "isPinned": {
      "type": "boolean",
      "default": false,
      "description": "Indique si la note est épinglée dans l'interface."
    },

    "tags": {
      "type": "array",
      "description": "Tags de classification (indice, background, secret...).",
      "items": { "type": "string" }
    },

    "visibility": {
      "type": "object",
      "description": "Détails fins de visibilité si besoin.",
      "properties": {
        "visibleToUserIds": {
          "type": "array",
          "description": "Liste explicite de users qui peuvent voir cette note (pour certains cas publics/groupes).",
          "items": { "type": "string" }
        },
        "visibleToAllPlayers": {
          "type": "boolean",
          "default": false,
          "description": "Note visible à tous les joueurs de la table (en plus du MJ)."
        }
      },
      "additionalProperties": true
    },

    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "Date de création."
    },

    "updatedAt": {
      "type": ["string", "null"],
      "format": "date-time",
      "description": "Dernière modification."
    },

    "sync": {
      "type": "object",
      "description": "Infos de synchronisation offline-first.",
      "properties": {
        "lastSyncedAt": {
          "type": ["string", "null"],
          "format": "date-time"
        },
        "lastModifiedBy": {
          "type": ["string", "null"],
          "description": "Dernier utilisateur ayant modifié la note."
        },
        "isEncryptedForOwnerOnly": {
          "type": "boolean",
          "default": false,
          "description": "Indique si le contenu est chiffré côté serveur (notes privées joueur)."
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
