# Backend API URL (sans slash final)
VITE_API_BASE_URL=https://api.nexusforge.en-ligne.fr

# Option legacy (laisser true)
VITE_BACKEND_ENABLED=true

# Transport de synchronisation
# http recommandé en production
VITE_SYNC_TRANSPORT=http
