# Active la connexion backend (si false ou absent => mode local/mock)
VITE_BACKEND_ENABLED=true

# URL publique de l'API backend (sans slash final), ex:
# https://api.nexusforge.en-ligne.fr
VITE_API_BASE_URL=

# Transport de synchronisation
# mock | http
VITE_SYNC_TRANSPORT=http
